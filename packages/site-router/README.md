# the router used by the implrust

This crate performs the codegen that transforms the markdown files into rsx.

Unfortunately, the codegen takes a while, so we needed to move it out into its own crate.

